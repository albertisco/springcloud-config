spring:
  application:
    name: gymapp
server:
  port: 8085
  servlet:
    context-path: /gymapp
eureka:
  instance:
    preferIpAddress: true
  client:
    region: default
    registryFetchIntervalSeconds: 15
    serviceUrl:
      defaultZone: http://localhost:8083/eureka/
