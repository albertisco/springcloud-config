spring:
  application:
    name: discovery
server:
  port: 8083
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8083/eureka/
    register-with-eureka: false
    fetch-registry: false
